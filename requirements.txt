# Core ML
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Transformers & NLP
transformers>=4.30.0
tokenizers>=0.13.0
sentencepiece>=0.1.99

# Data processing
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.2.0
pyyaml>=6.0

# Audio
TTS>=0.22.0
openai-whisper>=20230314

# Evaluation
rouge-score>=0.1.2
nltk>=3.8.0
jiwer>=3.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# Gradio (for demo)
gradio>=5.0.0

# Hosted LLM providers for gloss to English (src/inference/gloss_to_text.py).
# Cerebras is the primary and is called over plain HTTP, so it needs only
# requests. groq is the fallback's SDK: without it the fallback is unavailable,
# and a Cerebras failure drops the output to the rule-based converter, which is
# noticeably worse ("Me name what?" rather than "What is my name?").
# Verified together with requests 2.32.5 and groq 1.6.0.
requests>=2.31.0
groq>=1.6.0
